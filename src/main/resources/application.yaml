spring:
  application:
    name: randomizer

  datasource:
    url: jdbc:mysql://localhost:3306/dw_randomizer_db
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  main:
    web-application-type: none
    banner-mode: off